# Comma-separated list of origins allowed to call the API.
# In production, set this to your deployed frontend domain.
# Example: ALLOWED_ORIGINS=https://quizler.example.com
ALLOWED_ORIGINS=http://localhost:8000
